<Window x:Class="CrispyBills.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Settings"
        Height="280"
        Width="440"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="{DynamicResource WindowBackgroundBrush}"
        Foreground="{DynamicResource WindowForegroundBrush}">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Text="Display language" FontWeight="Bold" Margin="0,0,0,8"/>
        <TextBlock Grid.Row="1" TextWrapping="Wrap" Foreground="{DynamicResource WindowForegroundBrush}" Opacity="0.85"
                   Text="Applies number and date formats. Restart the app if some labels do not refresh immediately."
                   Margin="0,0,0,10"/>
        <ComboBox x:Name="CultureCombo" Grid.Row="2" VerticalAlignment="Top" MinHeight="28" Margin="0,0,0,12"
                  Background="{DynamicResource ControlBackgroundBrush}"
                  Foreground="{DynamicResource ControlForegroundBrush}"/>
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="OK" Width="88" Margin="0,0,8,0" Click="Ok_Click" IsDefault="True"/>
            <Button Content="Cancel" Width="88" IsCancel="True" Click="Cancel_Click"/>
        </StackPanel>
    </Grid>
</Window>
