<Window x:Class="CrispyBills.DiagnosticsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Diagnostics"
        Height="560"
        Width="640"
        WindowStartupLocation="CenterOwner"
        MinHeight="320"
        MinWidth="480"
        Background="{DynamicResource WindowBackgroundBrush}"
        Foreground="{DynamicResource WindowForegroundBrush}">
    <Grid Margin="14">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock TextWrapping="Wrap" Margin="0,0,0,10" Opacity="0.9"
                   Text="Data locations and quick access. Logs are written when non-fatal errors are recorded."/>
        <TextBox x:Name="PathsBox" Grid.Row="1" IsReadOnly="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto"
                 FontFamily="Consolas" FontSize="12"
                 Background="{DynamicResource ControlBackgroundBrush}"
                 Foreground="{DynamicResource ControlForegroundBrush}"/>
        <TextBlock Grid.Row="2" Text="Recent log entries" FontWeight="Bold" Margin="0,12,0,4"/>
        <TextBox x:Name="LogBox" Grid.Row="3" IsReadOnly="True" TextWrapping="Wrap"
                 VerticalScrollBarVisibility="Auto" FontFamily="Consolas" FontSize="11"
                 Background="{DynamicResource ControlBackgroundBrush}"
                 Foreground="{DynamicResource ControlForegroundBrush}"/>
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="Open data folder" Width="140" Margin="0,0,8,0" Click="OpenData_Click"/>
            <Button Content="Open backups folder" Width="160" Margin="0,0,8,0" Click="OpenBackups_Click"/>
            <Button Content="Run Integrity Check" Width="160" Margin="0,0,8,0" Click="RunIntegrityCheck_Click"/>
            <Button Content="Close" Width="88" IsCancel="True" IsDefault="True" Click="Close_Click"/>
        </StackPanel>
    </Grid>
</Window>
